registry=https://registry.npmjs.org/

# pnpm hardening
# Solo se ejecutan scripts de paquetes listados en package.json → pnpm.onlyBuiltDependencies
# (esto bloquea postinstall arbitrarios de transitivas, vector típico de supply-chain)
strict-peer-dependencies=false
resolution-mode=highest
audit-level=high
